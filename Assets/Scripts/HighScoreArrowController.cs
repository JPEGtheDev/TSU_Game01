using UnityEngine;
using System.Collections;

public class HighScoreArrowController : MonoBehaviour
{
	// this is used as a cheap workaround to select the arrow and have it controlled by the parent gameObject
		
}
